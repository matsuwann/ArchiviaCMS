export default function AdminDashboardPage() {
  return (
    <div>
      <h2 className="text-2xl font-bold mb-4">Welcome, Admin!</h2>
      <p>Select a management option from the navigation.</p>
      {/* Later, you'll add links here to /admin/users, /admin/documents, etc. */}
    </div>
  );
}